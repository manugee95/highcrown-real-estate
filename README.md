
  # Review attached files

  This is a code bundle for Review attached files. The original project is available at https://www.figma.com/design/JKXCURyOQMqFTwQt6MXhDG/Review-attached-files.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  